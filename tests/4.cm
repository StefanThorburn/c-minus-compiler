/* 
Errors:
- argument list is not valid in the function call
- The boolean expression on line 16 has an invalid right operand
- Invalid condition expression for the 'if' statement
- Invalid condition expression for the 'while' loop
 */

int gcd(int u, int v) {
  if (v == 0) return u;
  else return gcd(v, u - u/v*v);
}

int test (int u, int v) {
  int test;
  test = (u + w) > (%u);

  if(%u + v) u+ 1;

  while(%v!=null) return v;
}