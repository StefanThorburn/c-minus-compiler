/* 
Errors:
- argument list is not valid in the function call
- checks if the right side of the simple expression has an error
- checks fot invalid expression inside IF -> IF(expression)
- - checks fot invalid expression inside WHILE -> WHILE(expression)
 */

int gcd(int u, int v) {
  if (v == 0) return u;
  else return gcd(%v, u - u/v*v);
}

int test(int u, int v){
  int test;
  test = (u + w) > (%u);

  if(%u + v){
    u++;
  }

  while(%v!=null){
    return v;
  }
}