/*
Errors (in order):
- Variable of type 'void'
In main:
- Attempting to assign to constant '3'
- int frog > 5; -- can't have an expression as var name
- Loop condition is not a valid expression
- else without corresponding 'if'
- Attempt to access an invalid array index
In gcd:
- outer if has invalid condition
*/

int globalVar;
void badGlobalVar;

void main (void) {
    int frog;
    int cat;
    frog = 3;
    5 = 15 / frog;
    int frog > 5;

    while (frog < 6 < 2) {
        else frog = frog + 1;        
    } 

    int frogs[10];
    frogs[0] = frog + 1;
    frogs[n] = gcd(frog, 14);
}

int gcd(int u, int v) {
    if (u == int var + 3) {
        if (v == 0) return u;
        else return gcd(v, u - u/v*v);
    }
}