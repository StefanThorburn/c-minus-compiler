/*
Errors:
- the array index is invalid (consists of an expression statement rather than an expression)
- the function call is invalid (C- does not support modulus and so it is an error token)
*/
void main (void) {
   int frog;
   int frogArray[10];

   frog = frogArray[frog < 3;];
   frogArray[1] = func(frog % 2);
}

int func (int num) {
   return num + 1;
}